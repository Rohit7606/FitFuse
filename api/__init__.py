"""FitFuse API — thin, stateless HTTP shell over engine/ and market/.

The server holds no session state (AGENTS.md §3.4).
The client sends the full MarketScenario with every request.

Owner: Person B
Reviewer: Person A
"""
